#include <stdio.h>
int pattern(int n);
int main(){
    int n;
    printf("Enter the value of n:");
    scanf("%d",&n);
    return 0;
}
int pattern(int n){
    if(n == 1){
        
    }

}